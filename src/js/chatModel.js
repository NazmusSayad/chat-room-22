

/* startChatLoop(
  ({ data }) => {
    console.log(data)
    document.body.innerHTML += data.name + ": " + data.msg + "<br/>"
  },
  (e) => {
    console.log(e)
    document.body.innerHTML += "<br/>OPS Network gone!<br/>"
  }
) */
